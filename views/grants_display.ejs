<!DOCTYPE html>
<html>
<head>
  <title>Grant List</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
      background-color: #f7f7f7;
    }

    h1 {
      font-size: 2rem;
      font-weight: bold;
      color: #333;
      text-align: center;
      padding: 2rem 0;
      background-color: #ffc107;
      border-bottom: 1px solid #ddd;
    }

    ul {
      list-style: none;
      margin: 0;
      padding: 0;
    }

    li {
      margin: 1rem;
      padding: 1rem;
      border-bottom: 1px solid #ddd;
      background-color: #fff;
    }

    li:nth-child(even) {
      background-color: #f7f7f7;
    }

    a {
      display: block;
      padding: 0.5rem 1rem;
      background-color: #337ab7;
      color: #fff;
      text-decoration: none;
      border-radius: 4px;
      transition: background-color 0.2s ease;
    }

    a:hover {
      background-color: #23527c;
    }
  </style>
</head>
<body>
  <h1>Grant List</h1>
  <ul>
    <% grants.forEach(grant => { %>
      <li><%= grant.stud_id %> - <%= grant.amount %> - <%= grant.type %> - <%= grant.awarddate %></li>
    <% }); %>
  </ul>
  <br>
  <a href="/">Back to Home</a>
</body>
</html>
